'use strict';

const config = {
  letters: "../characters/letters.json",
  Column: "../column.js",
  letters_compress: "../characters/letters_compress.json",
  animation: "../background.js",
};

export default config;
